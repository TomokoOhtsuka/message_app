h1 ユーザー登録

= form_for @user, url: users_path do |f|
  = render 'form', form: f

/ _form.html.slimに移動させたs
/ = form_for(@user) do |f|
/   = f.label :name
/   = f.text_field :name

/   = f.label :email
/   = f.text_field :email

/   = f.label :password
/   = f.password_field :password

/   = f.label :password_confirmation
/   = f.password_field :password_confirmation
  
/   = f.submit 'アカウント登録'

